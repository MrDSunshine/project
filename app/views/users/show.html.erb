<div class="page-header">
  <h1>Welcome to <%= @user.username %>'s page</h1>
</div>

<div class="row">
  <div class="col-md-4 col-md-offset-4 text-center">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>
<h3 class="text-center"><%= @user.username %>'s posts</h3>

<div class="text-center">
  <%= will_paginate @posts %>
</div>

<%= render @posts %>

<div class="text-center">
  <%= will_paginate @posts %>
</div>
